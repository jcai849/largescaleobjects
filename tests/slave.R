debug(largeScaleR::unstub)
largeScaleR::worker(comms=structure(list(host='localhost', port=6379L, user=NULL,
					 pass=NULL, dbpass=NULL),
				    class=c("commsProcess", "process")), 
		    host='localhost',
		    port=largeScaleR::port(),
		    stopOnError=TRUE,
		    verbose=TRUE)

